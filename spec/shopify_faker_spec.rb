require 'spec_helper'

describe ShopifyFaker do
  it 'has a version number' do
    expect(ShopifyFaker::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
